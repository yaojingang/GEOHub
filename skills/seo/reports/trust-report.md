# Trust Report

The deterministic runtime reads one bounded regular JSON file and writes one atomic Artifact Bus run. It performs no network access or live mutation, rejects credential-bearing URLs, uses strict finite JSON, and preserves supplied evidence without converting it into unsupported findings. Live website access and authenticated search data require a separate user-authorized host capability.
