# Knowledge output contract

The run contains `knowledge-graph.json`, `knowledge-query-result.json`, `evidence-ledger.json`, `quality-report.json`, `run-lineage.json`, `run-manifest.json`, and the input snapshot.

Every graph entity and relation exposes source IDs. Coverage contains raw source, entity, and relation counts. Conflicts remain unresolved until a reviewer supplies an approved source update.
